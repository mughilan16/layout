export default function FileUpload(props:
    {
        acceptedFileTypes: string[],
        maxFileSelect: number,
        maxFileSizeInMb: number,
    }) {
    return (
        <>
        </>
    )
}
